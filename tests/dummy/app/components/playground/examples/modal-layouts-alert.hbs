{{!
    Modal::Layouts::Alert example adapter. Opened through `modalsManager.alert()`, which is how the
    documentation instructs consumers to reach this layout.
}}
<div class="pg-fixture-anchor">
    <Button @text="Open the alert dialog" @type="primary" @onClick={{this.open}} data-test-open-modal />
    <p class="pg-fixture-note">Shown through <code>modalsManager.alert()</code>.</p>
</div>
