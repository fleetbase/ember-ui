{{!
    Modal::Layouts::BulkAction example adapter. Opened through `modalsManager.bulk()`, which is how the
    documentation instructs consumers to reach this layout.
}}
<div class="pg-fixture-anchor">
    <Button @text="Open the bulk action dialog" @type="primary" @onClick={{this.open}} data-test-open-modal />
    <p class="pg-fixture-note">Shown through <code>modalsManager.bulk()</code>.</p>
</div>
