{{!
    Modal::Layouts::Confirm example adapter. Opened through `modalsManager.confirm()`, which is how the
    documentation instructs consumers to reach this layout.
}}
<div class="pg-fixture-anchor">
    <Button @text="Open the confirm dialog" @type="primary" @onClick={{this.open}} data-test-open-modal />
    <p class="pg-fixture-note">Shown through <code>modalsManager.confirm()</code>.</p>
</div>
