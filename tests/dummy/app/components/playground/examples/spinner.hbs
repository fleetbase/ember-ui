{{! Spinner example adapter. }}
<Spinner
    @message={{if @values.message @values.message null}}
    @size={{@values.size}}
    @wrapperClass={{@values.wrapperClass}}
    @iconClass={{@values.iconClass}}
/>
