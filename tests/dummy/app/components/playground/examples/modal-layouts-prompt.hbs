{{!
    Modal::Layouts::Prompt example adapter. Opened through `modalsManager.prompt()`, which is how the
    documentation instructs consumers to reach this layout.
}}
<div class="pg-fixture-anchor">
    <Button @text="Open the prompt dialog" @type="primary" @onClick={{this.open}} data-test-open-modal />
    <p class="pg-fixture-note">Shown through <code>modalsManager.prompt()</code>.</p>
</div>
