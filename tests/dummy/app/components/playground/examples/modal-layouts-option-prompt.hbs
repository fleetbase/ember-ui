{{!
    Modal::Layouts::OptionPrompt example adapter. Opened through `modalsManager.userSelectOption()`, which is how the
    documentation instructs consumers to reach this layout.
}}
<div class="pg-fixture-anchor">
    <Button @text="Open the option prompt dialog" @type="primary" @onClick={{this.open}} data-test-open-modal />
    <p class="pg-fixture-note">Shown through <code>modalsManager.userSelectOption()</code>.</p>
</div>
