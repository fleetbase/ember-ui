{{!
    Modal::Layouts::Progress example adapter. Opened through `modalsManager.progress()`, which is how the
    documentation instructs consumers to reach this layout.
}}
<div class="pg-fixture-anchor">
    <Button @text="Open the progress dialog" @type="primary" @onClick={{this.open}} data-test-open-modal />
    <p class="pg-fixture-note">Shown through <code>modalsManager.progress()</code>.</p>
</div>
