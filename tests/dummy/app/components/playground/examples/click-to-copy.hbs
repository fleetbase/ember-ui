{{!
    ClickToCopy example adapter. The clipboard write is attempted for real; a browser may refuse
    it without a user gesture or permission.
}}
<ClickToCopy @value={{@values.value}} @labelText={{@values.labelText}} />
