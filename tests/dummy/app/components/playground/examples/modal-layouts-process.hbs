{{!
    Modal::Layouts::Process example adapter. Opened through `modalsManager.process()`, which is how the
    documentation instructs consumers to reach this layout.
}}
<div class="pg-fixture-anchor">
    <Button @text="Open the process dialog" @type="primary" @onClick={{this.open}} data-test-open-modal />
    <p class="pg-fixture-note">Shown through <code>modalsManager.process()</code>.</p>
</div>
