{{! Layout::MobileNavbar example adapter: a mobile media stub and a local menu fixture. }}
<Layout::MobileNavbar @menuItems={{this.menuItems}} @size={{@values.size}} @onSetup={{fn (or @onEvent (noop)) "onSetup"}} />
