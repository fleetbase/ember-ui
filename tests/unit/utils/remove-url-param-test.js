import removeUrlParam from 'dummy/utils/remove-url-param';
import { module, test } from 'qunit';

module('Unit | Utility | removeUrlParam', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = removeUrlParam();
        assert.ok(result);
    });
});
