import decoratorsUsesTransition from 'dummy/utils/decorators/uses-transition';
import { module, test } from 'qunit';

module('Unit | Utility | decorators/uses-transition', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = decoratorsUsesTransition();
    assert.ok(result);
  });
});
