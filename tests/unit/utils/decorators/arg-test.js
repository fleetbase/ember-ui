import decoratorsArg from 'dummy/utils/decorators/arg';
import { module, test } from 'qunit';

module('Unit | Utility | decorators/arg', function () {
    test('it works', function (assert) {
        let result = decoratorsArg();
        assert.ok(true);
    });
});
