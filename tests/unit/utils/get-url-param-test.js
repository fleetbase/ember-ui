import getUrlParam from 'dummy/utils/get-url-param';
import { module, test } from 'qunit';

module('Unit | Utility | getUrlParam', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getUrlParam();
        assert.ok(result);
    });
});
