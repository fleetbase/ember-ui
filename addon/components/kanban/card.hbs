{{! template-lint-disable require-presentational-children }}
<div
    class={{this.cardClasses}}
    draggable={{this.isDraggable}}
    {{!-- The component's own actions forward the CARD alongside the event and guard the
        absent handler themselves; binding @onDragStart directly handed a direct consumer of
        <Kanban::Card> only the raw event. --}}
    {{on "dragstart" this.onDragStart}}
    {{on "dragend" this.onDragEnd}}
    {{on "click" this.onClick}}
    data-card-id={{this.card.id}}
    role="button"
    tabindex="0"
>
    {{#if @template}}
        {{! The card's own actions are handed to a custom template: they are the only route by
            which @onCardUpdate and @onCardDelete can ever fire — see DEFECTS #25. }}
        {{component @template card=this.card onUpdate=this.onUpdate onDelete=this.onDelete}}
    {{else}}
        <div class="kanban-card-content">
            <h4 class="kanban-card-title">{{this.card.title}}</h4>
            
            {{#if this.card.description}}
                <p class="kanban-card-description">{{this.card.description}}</p>
            {{/if}}
        </div>
    {{/if}}
</div>
