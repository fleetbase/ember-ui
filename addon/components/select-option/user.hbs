<Resource::SelectOption
    @option={{or @option @model @user}}
    @resourceType="user"
    @compact={{@compact}}
    @title={{@title}}
    @details={{@details}}
    @photo={{@photo}}
    @fallbackPhoto={{@fallbackPhoto}}
    @icon={{@icon}}
    @shape={{@shape}}
    ...attributes
/>
