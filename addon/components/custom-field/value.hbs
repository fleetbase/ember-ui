<span class="custom-meta-field" ...attributes>
    {{#if this.isTextDisplay}}
        {{@value}}
    {{/if}}

    {{#if this.isBooleanInput}}
        <Badge @status={{if @metaField.value "success" ""}}>{{if @metaField.value "Yes" "No"}}</Badge>
    {{/if}}
    {{yield}}
</span>